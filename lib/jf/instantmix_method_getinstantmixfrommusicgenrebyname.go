// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"net/url"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetInstantMixFromMusicGenreByNameOperationResponse is the result of GetInstantMixFromMusicGenreByName.
type GetInstantMixFromMusicGenreByNameOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetInstantMixFromMusicGenreByNameOperationOptions holds the query and header parameters of GetInstantMixFromMusicGenreByName.
type GetInstantMixFromMusicGenreByNameOperationOptions struct {
	// Optional. Filter by user id, and attach user data.
	UserId string

	// Optional. The maximum number of records to return.
	Limit *int

	// Optional. Specify additional fields of information to return in the output.
	Fields []ItemFields

	// Optional. Include image information in output.
	EnableImages *bool

	// Optional. Include user data.
	EnableUserData *bool

	// Optional. The max number of images to return, per image type.
	ImageTypeLimit *int

	// Optional. The image types to include in the output.
	EnableImageTypes []ImageType
}

// ToHeaders returns the header parameters the options set.
func (o GetInstantMixFromMusicGenreByNameOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetInstantMixFromMusicGenreByNameOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	if o.Limit != nil {
		out.Append("limit", strconv.Itoa(*o.Limit))
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	if o.ImageTypeLimit != nil {
		out.Append("imageTypeLimit", strconv.Itoa(*o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	return &out
}

// GetInstantMixFromMusicGenreByName calls GET /MusicGenres/{name}/InstantMix. Creates an instant playlist based on a given genre.
//
// Deprecated: the document marks this operation deprecated.
func (c Client) GetInstantMixFromMusicGenreByName(ctx context.Context, name string, options GetInstantMixFromMusicGenreByNameOperationOptions) (result GetInstantMixFromMusicGenreByNameOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HTTPMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          fmt.Sprintf("/MusicGenres/%s/InstantMix", url.PathEscape(name)),
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}
