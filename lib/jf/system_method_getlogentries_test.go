// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"net/http"
	"slices"
	"testing"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

func TestOperationGetLogEntries(t *testing.T) {
	t.Parallel()

	c, s := newOperationServer(t, 200, "application/json", "{}")
	result, err := c.GetLogEntries(t.Context(), GetLogEntriesOperationOptions{
		StartIndex:    7,
		Limit:         7,
		MinDate:       "v-MinDate",
		MaxDate:       "v-MaxDate",
		HasUserId:     new(true),
		Name:          "v-Name",
		Overview:      "v-Overview",
		ShortOverview: "v-ShortOverview",
		Type:          "v-Type",
		ItemId:        "v-ItemId",
		Username:      "v-Username",
		Severity:      LogLevelTrace,
		SortBy:        []ActivityLogSortBy{ActivityLogSortByName, ActivityLogSortByLogSeverity},
		SortOrder:     []SortOrder{SortOrderAscending, SortOrderDescending},
	})
	if err != nil {
		t.Fatal(err)
	}
	r, body := s.only(t)
	_ = body
	expectRequest(t, r, http.MethodGet, "/System/ActivityLog/Entries")
	expectQuery(t, r, "startIndex", "7")
	expectQuery(t, r, "limit", "7")
	expectQuery(t, r, "minDate", "v-MinDate")
	expectQuery(t, r, "maxDate", "v-MaxDate")
	expectQuery(t, r, "hasUserId", "true")
	expectQuery(t, r, "name", "v-Name")
	expectQuery(t, r, "overview", "v-Overview")
	expectQuery(t, r, "shortOverview", "v-ShortOverview")
	expectQuery(t, r, "type", "v-Type")
	expectQuery(t, r, "itemId", "v-ItemId")
	expectQuery(t, r, "username", "v-Username")
	expectQuery(t, r, "severity", "Trace")
	expectQuery(t, r, "sortBy", "Name", "LogSeverity")
	expectQuery(t, r, "sortOrder", "Ascending", "Descending")
	if result.HttpResponse == nil || result.HttpResponse.StatusCode != 200 {
		t.Fatalf("HttpResponse = %+v", result.HttpResponse)
	}
	if result.Model == nil {
		t.Error("the model was not decoded")
	}

	// an answer that does not decode is an error, with the response
	c, _ = newOperationServer(t, 200, "application/json", "<html>")
	result, err = c.GetLogEntries(t.Context(), GetLogEntriesOperationOptions{
		StartIndex:    7,
		Limit:         7,
		MinDate:       "v-MinDate",
		MaxDate:       "v-MaxDate",
		HasUserId:     new(true),
		Name:          "v-Name",
		Overview:      "v-Overview",
		ShortOverview: "v-ShortOverview",
		Type:          "v-Type",
		ItemId:        "v-ItemId",
		Username:      "v-Username",
		Severity:      LogLevelTrace,
		SortBy:        []ActivityLogSortBy{ActivityLogSortByName, ActivityLogSortByLogSeverity},
		SortOrder:     []SortOrder{SortOrderAscending, SortOrderDescending},
	})
	if err == nil || client.StatusCode(err) != 0 || result.HttpResponse == nil {
		t.Errorf("an answer that does not decode = %v", err)
	}

	// a status the operation does not document is an error, with the response
	c, _ = newOperationServer(t, 418, "text/plain", "no")
	result, err = c.GetLogEntries(t.Context(), GetLogEntriesOperationOptions{
		StartIndex:    7,
		Limit:         7,
		MinDate:       "v-MinDate",
		MaxDate:       "v-MaxDate",
		HasUserId:     new(true),
		Name:          "v-Name",
		Overview:      "v-Overview",
		ShortOverview: "v-ShortOverview",
		Type:          "v-Type",
		ItemId:        "v-ItemId",
		Username:      "v-Username",
		Severity:      LogLevelTrace,
		SortBy:        []ActivityLogSortBy{ActivityLogSortByName, ActivityLogSortByLogSeverity},
		SortOrder:     []SortOrder{SortOrderAscending, SortOrderDescending},
	})
	if client.StatusCode(err) != 418 || result.HttpResponse == nil {
		t.Errorf("an undocumented status = %v, %+v", err, result.HttpResponse)
	}
}

func TestOperationGetLogEntriesComplete(t *testing.T) {
	t.Parallel()

	c, starts := pagedServer(t, "startIndex", "{}", 2)
	result, err := c.GetLogEntriesComplete(t.Context(), GetLogEntriesOperationOptions{Limit: 1})
	if err != nil {
		t.Fatal(err)
	}
	// a page of one from the start, then from the first item, then the total is reached
	if len(result.Items) != 2 || !slices.Equal(*starts, []string{"", "1"}) || result.LatestHttpResponse == nil {
		t.Errorf("Complete = %d items over pages starting %q", len(result.Items), *starts)
	}
}
