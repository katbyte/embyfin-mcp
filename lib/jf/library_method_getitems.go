// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetItemsOperationResponse is the result of GetItems.
type GetItemsOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetItemsOperationOptions holds the query and header parameters of GetItems.
type GetItemsOperationOptions struct {
	// The user id supplied as query parameter; this is required when not using an API key.
	UserId string

	// Optional filter by maximum official rating (PG, PG-13, TV-MA, etc).
	MaxOfficialRating string

	// Optional filter by items with theme songs.
	HasThemeSong *bool

	// Optional filter by items with theme videos.
	HasThemeVideo *bool

	// Optional filter by items with subtitles.
	HasSubtitles *bool

	// Optional filter by items with special features.
	HasSpecialFeature *bool

	// Optional filter by items with trailers.
	HasTrailer *bool

	// Optional. Return items that are siblings of a supplied item.
	AdjacentTo string

	// Optional filter by index number.
	IndexNumber int

	// Optional filter by parent index number.
	ParentIndexNumber int

	// Optional filter by items that have or do not have a parental rating.
	HasParentalRating *bool

	// Optional filter by items that are HD or not.
	IsHd *bool

	// Optional filter by items that are 4K or not.
	Is4K *bool

	// Optional. If specified, results will be filtered based on LocationType. This allows multiple, comma delimited.
	LocationTypes []LocationType

	// Optional. If specified, results will be filtered based on the LocationType. This allows multiple, comma delimited.
	ExcludeLocationTypes []LocationType

	// Optional filter by items that are missing episodes or not.
	IsMissing *bool

	// Optional filter by items that are unaired episodes or not.
	IsUnaired *bool

	// Optional filter by minimum community rating.
	MinCommunityRating float64

	// Optional filter by minimum critic rating.
	MinCriticRating float64

	// Optional. The minimum premiere date. Format = ISO.
	MinPremiereDate string

	// Optional. The minimum last saved date. Format = ISO.
	MinDateLastSaved string

	// Optional. The minimum last saved date for the current user. Format = ISO.
	MinDateLastSavedForUser string

	// Optional. The maximum premiere date. Format = ISO.
	MaxPremiereDate string

	// Optional filter by items that have an overview or not.
	HasOverview *bool

	// Optional filter by items that have an IMDb id or not.
	HasImdbId *bool

	// Optional filter by items that have a TMDb id or not.
	HasTmdbId *bool

	// Optional filter by items that have a TVDb id or not.
	HasTvdbId *bool

	// Optional filter for live tv movies.
	IsMovie *bool

	// Optional filter for live tv series.
	IsSeries *bool

	// Optional filter for live tv news.
	IsNews *bool

	// Optional filter for live tv kids.
	IsKids *bool

	// Optional filter for live tv sports.
	IsSports *bool

	// Optional. If specified, results will be filtered by excluding item ids. This allows multiple, comma delimited.
	ExcludeItemIds []string

	// Optional. The record index to start at. All items with a lower index will be dropped from the results.
	StartIndex int

	// Optional. The maximum number of records to return.
	Limit int

	// When searching within folders, this determines whether or not the search will be recursive. true/false.
	Recursive *bool

	// Optional. Filter based on a search term.
	SearchTerm string

	// Sort Order - Ascending, Descending.
	SortOrder []SortOrder

	// Specify this to localize the search to a specific item or folder. Omit to use the root.
	ParentId string

	// Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines.
	Fields []ItemFields

	// Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
	ExcludeItemTypes []BaseItemKind

	// Optional. If specified, results will be filtered based on the item type. This allows multiple, comma delimited.
	IncludeItemTypes []BaseItemKind

	// Optional. Specify additional filters to apply. This allows multiple, comma delimited. Options: IsFolder, IsNotFolder, IsUnplayed, IsPlayed, IsFavorite, IsResumable, Likes, Dislikes.
	Filters []ItemFilter

	// Optional filter by items that are marked as favorite, or not.
	IsFavorite *bool

	// Optional filter by MediaType. Allows multiple, comma delimited.
	MediaTypes []MediaType

	// Optional. If specified, results will be filtered based on those containing image types. This allows multiple, comma delimited.
	ImageTypes []ImageType

	// Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
	SortBy []ItemSortBy

	// Optional filter by items that are played, or not.
	IsPlayed *bool

	// Optional. If specified, results will be filtered based on genre. This allows multiple, pipe delimited.
	Genres []string

	// Optional. If specified, results will be filtered based on OfficialRating. This allows multiple, pipe delimited.
	OfficialRatings []string

	// Optional. If specified, results will be filtered based on tag. This allows multiple, pipe delimited.
	Tags []string

	// Optional. If specified, results will be filtered based on production year. This allows multiple, comma delimited.
	Years []int

	// Optional, include user data.
	EnableUserData *bool

	// Optional, the max number of images to return, per image type.
	ImageTypeLimit int

	// Optional. The image types to include in the output.
	EnableImageTypes []ImageType

	// Optional. If specified, results will be filtered to include only those containing the specified person.
	Person string

	// Optional. If specified, results will be filtered to include only those containing the specified person id.
	PersonIds []string

	// Optional. If specified, along with Person, results will be filtered to include only those containing the specified person and PersonType. Allows multiple, comma-delimited.
	PersonTypes []string

	// Optional. If specified, results will be filtered based on studio. This allows multiple, pipe delimited.
	Studios []string

	// Optional. If specified, results will be filtered based on artists. This allows multiple, pipe delimited.
	Artists []string

	// Optional. If specified, results will be filtered based on artist id. This allows multiple, pipe delimited.
	ExcludeArtistIds []string

	// Optional. If specified, results will be filtered to include only those containing the specified artist id.
	ArtistIds []string

	// Optional. If specified, results will be filtered to include only those containing the specified album artist id.
	AlbumArtistIds []string

	// Optional. If specified, results will be filtered to include only those containing the specified contributing artist id.
	ContributingArtistIds []string

	// Optional. If specified, results will be filtered based on album. This allows multiple, pipe delimited.
	Albums []string

	// Optional. If specified, results will be filtered based on album id. This allows multiple, pipe delimited.
	AlbumIds []string

	// Optional. If specific items are needed, specify a list of item id's to retrieve. This allows multiple, comma delimited.
	Ids []string

	// Optional filter by VideoType (videofile, dvd, bluray, iso). Allows multiple, comma delimited.
	VideoTypes []VideoType

	// Optional filter by minimum official rating (PG, PG-13, TV-MA, etc).
	MinOfficialRating string

	// Optional filter by items that are locked.
	IsLocked *bool

	// Optional filter by items that are placeholders.
	IsPlaceHolder *bool

	// Optional filter by items that have official ratings.
	HasOfficialRating *bool

	// Whether or not to hide items behind their boxsets.
	CollapseBoxSetItems *bool

	// Optional. Filter by the minimum width of the item.
	MinWidth int

	// Optional. Filter by the minimum height of the item.
	MinHeight int

	// Optional. Filter by the maximum width of the item.
	MaxWidth int

	// Optional. Filter by the maximum height of the item.
	MaxHeight int

	// Optional filter by items that are 3D, or not.
	Is3D *bool

	// Optional filter by Series Status. Allows multiple, comma delimited.
	SeriesStatus []SeriesStatus

	// Optional filter by items whose name is sorted equally or greater than a given input string.
	NameStartsWithOrGreater string

	// Optional filter by items whose name is sorted equally than a given input string.
	NameStartsWith string

	// Optional filter by items whose name is equally or lesser than a given input string.
	NameLessThan string

	// Optional. If specified, results will be filtered based on studio id. This allows multiple, pipe delimited.
	StudioIds []string

	// Optional. If specified, results will be filtered based on genre id. This allows multiple, pipe delimited.
	GenreIds []string

	// Optional. If specified, results will be filtered based on audio language. This allows multiple, comma delimited values.
	AudioLanguages []string

	// Optional. If specified, results will be filtered based on subtitle language. This allows multiple, comma delimited values.
	SubtitleLanguages []string

	// Optional. Enable the total record count.
	EnableTotalRecordCount *bool

	// Optional, include image information in output.
	EnableImages *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetItemsOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetItemsOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	if o.MaxOfficialRating != "" {
		out.Append("maxOfficialRating", o.MaxOfficialRating)
	}
	if o.HasThemeSong != nil {
		out.Append("hasThemeSong", strconv.FormatBool(*o.HasThemeSong))
	}
	if o.HasThemeVideo != nil {
		out.Append("hasThemeVideo", strconv.FormatBool(*o.HasThemeVideo))
	}
	if o.HasSubtitles != nil {
		out.Append("hasSubtitles", strconv.FormatBool(*o.HasSubtitles))
	}
	if o.HasSpecialFeature != nil {
		out.Append("hasSpecialFeature", strconv.FormatBool(*o.HasSpecialFeature))
	}
	if o.HasTrailer != nil {
		out.Append("hasTrailer", strconv.FormatBool(*o.HasTrailer))
	}
	if o.AdjacentTo != "" {
		out.Append("adjacentTo", o.AdjacentTo)
	}
	if o.IndexNumber != 0 {
		out.Append("indexNumber", strconv.Itoa(o.IndexNumber))
	}
	if o.ParentIndexNumber != 0 {
		out.Append("parentIndexNumber", strconv.Itoa(o.ParentIndexNumber))
	}
	if o.HasParentalRating != nil {
		out.Append("hasParentalRating", strconv.FormatBool(*o.HasParentalRating))
	}
	if o.IsHd != nil {
		out.Append("isHd", strconv.FormatBool(*o.IsHd))
	}
	if o.Is4K != nil {
		out.Append("is4K", strconv.FormatBool(*o.Is4K))
	}
	for _, v := range o.LocationTypes {
		out.Append("locationTypes", string(v))
	}
	for _, v := range o.ExcludeLocationTypes {
		out.Append("excludeLocationTypes", string(v))
	}
	if o.IsMissing != nil {
		out.Append("isMissing", strconv.FormatBool(*o.IsMissing))
	}
	if o.IsUnaired != nil {
		out.Append("isUnaired", strconv.FormatBool(*o.IsUnaired))
	}
	if o.MinCommunityRating != 0 {
		out.Append("minCommunityRating", strconv.FormatFloat(o.MinCommunityRating, 'f', -1, 64))
	}
	if o.MinCriticRating != 0 {
		out.Append("minCriticRating", strconv.FormatFloat(o.MinCriticRating, 'f', -1, 64))
	}
	if o.MinPremiereDate != "" {
		out.Append("minPremiereDate", o.MinPremiereDate)
	}
	if o.MinDateLastSaved != "" {
		out.Append("minDateLastSaved", o.MinDateLastSaved)
	}
	if o.MinDateLastSavedForUser != "" {
		out.Append("minDateLastSavedForUser", o.MinDateLastSavedForUser)
	}
	if o.MaxPremiereDate != "" {
		out.Append("maxPremiereDate", o.MaxPremiereDate)
	}
	if o.HasOverview != nil {
		out.Append("hasOverview", strconv.FormatBool(*o.HasOverview))
	}
	if o.HasImdbId != nil {
		out.Append("hasImdbId", strconv.FormatBool(*o.HasImdbId))
	}
	if o.HasTmdbId != nil {
		out.Append("hasTmdbId", strconv.FormatBool(*o.HasTmdbId))
	}
	if o.HasTvdbId != nil {
		out.Append("hasTvdbId", strconv.FormatBool(*o.HasTvdbId))
	}
	if o.IsMovie != nil {
		out.Append("isMovie", strconv.FormatBool(*o.IsMovie))
	}
	if o.IsSeries != nil {
		out.Append("isSeries", strconv.FormatBool(*o.IsSeries))
	}
	if o.IsNews != nil {
		out.Append("isNews", strconv.FormatBool(*o.IsNews))
	}
	if o.IsKids != nil {
		out.Append("isKids", strconv.FormatBool(*o.IsKids))
	}
	if o.IsSports != nil {
		out.Append("isSports", strconv.FormatBool(*o.IsSports))
	}
	for _, v := range o.ExcludeItemIds {
		out.Append("excludeItemIds", v)
	}
	if o.StartIndex != 0 {
		out.Append("startIndex", strconv.Itoa(o.StartIndex))
	}
	if o.Limit != 0 {
		out.Append("limit", strconv.Itoa(o.Limit))
	}
	if o.Recursive != nil {
		out.Append("recursive", strconv.FormatBool(*o.Recursive))
	}
	if o.SearchTerm != "" {
		out.Append("searchTerm", o.SearchTerm)
	}
	for _, v := range o.SortOrder {
		out.Append("sortOrder", string(v))
	}
	if o.ParentId != "" {
		out.Append("parentId", o.ParentId)
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	for _, v := range o.ExcludeItemTypes {
		out.Append("excludeItemTypes", string(v))
	}
	for _, v := range o.IncludeItemTypes {
		out.Append("includeItemTypes", string(v))
	}
	for _, v := range o.Filters {
		out.Append("filters", string(v))
	}
	if o.IsFavorite != nil {
		out.Append("isFavorite", strconv.FormatBool(*o.IsFavorite))
	}
	for _, v := range o.MediaTypes {
		out.Append("mediaTypes", string(v))
	}
	for _, v := range o.ImageTypes {
		out.Append("imageTypes", string(v))
	}
	for _, v := range o.SortBy {
		out.Append("sortBy", string(v))
	}
	if o.IsPlayed != nil {
		out.Append("isPlayed", strconv.FormatBool(*o.IsPlayed))
	}
	for _, v := range o.Genres {
		out.Append("genres", v)
	}
	for _, v := range o.OfficialRatings {
		out.Append("officialRatings", v)
	}
	for _, v := range o.Tags {
		out.Append("tags", v)
	}
	for _, v := range o.Years {
		out.Append("years", strconv.Itoa(v))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	if o.ImageTypeLimit != 0 {
		out.Append("imageTypeLimit", strconv.Itoa(o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	if o.Person != "" {
		out.Append("person", o.Person)
	}
	for _, v := range o.PersonIds {
		out.Append("personIds", v)
	}
	for _, v := range o.PersonTypes {
		out.Append("personTypes", v)
	}
	for _, v := range o.Studios {
		out.Append("studios", v)
	}
	for _, v := range o.Artists {
		out.Append("artists", v)
	}
	for _, v := range o.ExcludeArtistIds {
		out.Append("excludeArtistIds", v)
	}
	for _, v := range o.ArtistIds {
		out.Append("artistIds", v)
	}
	for _, v := range o.AlbumArtistIds {
		out.Append("albumArtistIds", v)
	}
	for _, v := range o.ContributingArtistIds {
		out.Append("contributingArtistIds", v)
	}
	for _, v := range o.Albums {
		out.Append("albums", v)
	}
	for _, v := range o.AlbumIds {
		out.Append("albumIds", v)
	}
	for _, v := range o.Ids {
		out.Append("ids", v)
	}
	for _, v := range o.VideoTypes {
		out.Append("videoTypes", string(v))
	}
	if o.MinOfficialRating != "" {
		out.Append("minOfficialRating", o.MinOfficialRating)
	}
	if o.IsLocked != nil {
		out.Append("isLocked", strconv.FormatBool(*o.IsLocked))
	}
	if o.IsPlaceHolder != nil {
		out.Append("isPlaceHolder", strconv.FormatBool(*o.IsPlaceHolder))
	}
	if o.HasOfficialRating != nil {
		out.Append("hasOfficialRating", strconv.FormatBool(*o.HasOfficialRating))
	}
	if o.CollapseBoxSetItems != nil {
		out.Append("collapseBoxSetItems", strconv.FormatBool(*o.CollapseBoxSetItems))
	}
	if o.MinWidth != 0 {
		out.Append("minWidth", strconv.Itoa(o.MinWidth))
	}
	if o.MinHeight != 0 {
		out.Append("minHeight", strconv.Itoa(o.MinHeight))
	}
	if o.MaxWidth != 0 {
		out.Append("maxWidth", strconv.Itoa(o.MaxWidth))
	}
	if o.MaxHeight != 0 {
		out.Append("maxHeight", strconv.Itoa(o.MaxHeight))
	}
	if o.Is3D != nil {
		out.Append("is3D", strconv.FormatBool(*o.Is3D))
	}
	for _, v := range o.SeriesStatus {
		out.Append("seriesStatus", string(v))
	}
	if o.NameStartsWithOrGreater != "" {
		out.Append("nameStartsWithOrGreater", o.NameStartsWithOrGreater)
	}
	if o.NameStartsWith != "" {
		out.Append("nameStartsWith", o.NameStartsWith)
	}
	if o.NameLessThan != "" {
		out.Append("nameLessThan", o.NameLessThan)
	}
	for _, v := range o.StudioIds {
		out.Append("studioIds", v)
	}
	for _, v := range o.GenreIds {
		out.Append("genreIds", v)
	}
	for _, v := range o.AudioLanguages {
		out.Append("audioLanguages", v)
	}
	for _, v := range o.SubtitleLanguages {
		out.Append("subtitleLanguages", v)
	}
	if o.EnableTotalRecordCount != nil {
		out.Append("enableTotalRecordCount", strconv.FormatBool(*o.EnableTotalRecordCount))
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	return &out
}

// GetItems calls GET /Items. Gets items based on a query.
func (c Client) GetItems(ctx context.Context, options GetItemsOperationOptions) (result GetItemsOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HttpMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          "/Items",
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}

// GetItemsCompleteResult is every result GetItemsComplete loaded.
type GetItemsCompleteResult struct {
	LatestHttpResponse *http.Response
	Items              []BaseItemDto
}

// GetItemsComplete calls GetItems page by page from options.StartIndex until every
// result is loaded. options.Limit is the page size, client.DefaultPageSize when
// unset.
func (c Client) GetItemsComplete(ctx context.Context, options GetItemsOperationOptions) (result GetItemsCompleteResult, err error) {
	if options.Limit <= 0 {
		options.Limit = client.DefaultPageSize
	}
	for {
		var page GetItemsOperationResponse
		page, err = c.GetItems(ctx, options)
		result.LatestHttpResponse = page.HttpResponse
		if err != nil {
			err = fmt.Errorf("loading results: %w", err)
			return
		}
		if page.Model == nil || len(page.Model.Items) == 0 {
			return
		}
		result.Items = append(result.Items, page.Model.Items...)
		options.StartIndex += len(page.Model.Items)
		// a short page is the last; so is reaching the total, when the server
		// reports one (some Emby lists report 0 whatever they hold)
		if len(page.Model.Items) < options.Limit || page.Model.TotalRecordCount > 0 && options.StartIndex >= page.Model.TotalRecordCount {
			return
		}
	}
}
