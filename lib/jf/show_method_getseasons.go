// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"net/url"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetSeasonsOperationResponse is the result of GetSeasons.
type GetSeasonsOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetSeasonsOperationOptions holds the query and header parameters of GetSeasons.
type GetSeasonsOperationOptions struct {
	// The user id.
	UserId string

	// Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
	Fields []ItemFields

	// Optional. Filter by special season.
	IsSpecialSeason *bool

	// Optional. Filter by items that are missing episodes or not.
	IsMissing *bool

	// Optional. Return items that are siblings of a supplied item.
	AdjacentTo string

	// Optional. Include image information in output.
	EnableImages *bool

	// Optional. The max number of images to return, per image type.
	ImageTypeLimit *int

	// Optional. The image types to include in the output.
	EnableImageTypes []ImageType

	// Optional. Include user data.
	EnableUserData *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetSeasonsOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetSeasonsOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	if o.IsSpecialSeason != nil {
		out.Append("isSpecialSeason", strconv.FormatBool(*o.IsSpecialSeason))
	}
	if o.IsMissing != nil {
		out.Append("isMissing", strconv.FormatBool(*o.IsMissing))
	}
	if o.AdjacentTo != "" {
		out.Append("adjacentTo", o.AdjacentTo)
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.ImageTypeLimit != nil {
		out.Append("imageTypeLimit", strconv.Itoa(*o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	return &out
}

// GetSeasons calls GET /Shows/{seriesId}/Seasons. Gets seasons for a tv series.
func (c Client) GetSeasons(ctx context.Context, seriesId string, options GetSeasonsOperationOptions) (result GetSeasonsOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HTTPMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          fmt.Sprintf("/Shows/%s/Seasons", url.PathEscape(seriesId)),
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}
