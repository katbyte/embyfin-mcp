// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"net/http"
	"slices"
	"testing"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

func TestOperationGetPersons(t *testing.T) {
	t.Parallel()

	c, s := newOperationServer(t, 200, "application/json", "{}")
	result, err := c.GetPersons(t.Context(), GetPersonsOperationOptions{
		StartIndex:              new(7),
		Limit:                   new(7),
		SearchTerm:              "v-SearchTerm",
		NameStartsWith:          "v-NameStartsWith",
		NameLessThan:            "v-NameLessThan",
		NameStartsWithOrGreater: "v-NameStartsWithOrGreater",
		Fields:                  []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		Filters:                 []ItemFilter{ItemFilterIsFolder, ItemFilterIsFavoriteOrLikes},
		IsFavorite:              new(true),
		EnableUserData:          new(true),
		ImageTypeLimit:          new(7),
		EnableImageTypes:        []ImageType{ImageTypePrimary, ImageTypeProfile},
		ExcludePersonTypes:      []string{"a", "b"},
		PersonTypes:             []string{"a", "b"},
		ParentId:                "v-ParentId",
		AppearsInItemId:         "v-AppearsInItemId",
		UserId:                  "v-UserId",
		EnableImages:            new(true),
	})
	if err != nil {
		t.Fatal(err)
	}
	r, body := s.only(t)
	_ = body
	expectRequest(t, r, http.MethodGet, "/Persons")
	expectQuery(t, r, "startIndex", "7")
	expectQuery(t, r, "limit", "7")
	expectQuery(t, r, "searchTerm", "v-SearchTerm")
	expectQuery(t, r, "nameStartsWith", "v-NameStartsWith")
	expectQuery(t, r, "nameLessThan", "v-NameLessThan")
	expectQuery(t, r, "nameStartsWithOrGreater", "v-NameStartsWithOrGreater")
	expectQuery(t, r, "fields", "AirTime", "SpecialFeatureCount")
	expectQuery(t, r, "filters", "IsFolder", "IsFavoriteOrLikes")
	expectQuery(t, r, "isFavorite", "true")
	expectQuery(t, r, "enableUserData", "true")
	expectQuery(t, r, "imageTypeLimit", "7")
	expectQuery(t, r, "enableImageTypes", "Primary", "Profile")
	expectQuery(t, r, "excludePersonTypes", "a", "b")
	expectQuery(t, r, "personTypes", "a", "b")
	expectQuery(t, r, "parentId", "v-ParentId")
	expectQuery(t, r, "appearsInItemId", "v-AppearsInItemId")
	expectQuery(t, r, "userId", "v-UserId")
	expectQuery(t, r, "enableImages", "true")
	if result.HttpResponse == nil || result.HttpResponse.StatusCode != 200 {
		t.Fatalf("HttpResponse = %+v", result.HttpResponse)
	}
	if result.Model == nil {
		t.Error("the model was not decoded")
	}

	// an answer that does not decode is an error, with the response
	c, _ = newOperationServer(t, 200, "application/json", "<html>")
	result, err = c.GetPersons(t.Context(), GetPersonsOperationOptions{
		StartIndex:              new(7),
		Limit:                   new(7),
		SearchTerm:              "v-SearchTerm",
		NameStartsWith:          "v-NameStartsWith",
		NameLessThan:            "v-NameLessThan",
		NameStartsWithOrGreater: "v-NameStartsWithOrGreater",
		Fields:                  []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		Filters:                 []ItemFilter{ItemFilterIsFolder, ItemFilterIsFavoriteOrLikes},
		IsFavorite:              new(true),
		EnableUserData:          new(true),
		ImageTypeLimit:          new(7),
		EnableImageTypes:        []ImageType{ImageTypePrimary, ImageTypeProfile},
		ExcludePersonTypes:      []string{"a", "b"},
		PersonTypes:             []string{"a", "b"},
		ParentId:                "v-ParentId",
		AppearsInItemId:         "v-AppearsInItemId",
		UserId:                  "v-UserId",
		EnableImages:            new(true),
	})
	if err == nil || client.StatusCode(err) != 0 || result.HttpResponse == nil {
		t.Errorf("an answer that does not decode = %v", err)
	}

	// a status the operation does not document is an error, with the response
	c, _ = newOperationServer(t, 418, "text/plain", "no")
	result, err = c.GetPersons(t.Context(), GetPersonsOperationOptions{
		StartIndex:              new(7),
		Limit:                   new(7),
		SearchTerm:              "v-SearchTerm",
		NameStartsWith:          "v-NameStartsWith",
		NameLessThan:            "v-NameLessThan",
		NameStartsWithOrGreater: "v-NameStartsWithOrGreater",
		Fields:                  []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		Filters:                 []ItemFilter{ItemFilterIsFolder, ItemFilterIsFavoriteOrLikes},
		IsFavorite:              new(true),
		EnableUserData:          new(true),
		ImageTypeLimit:          new(7),
		EnableImageTypes:        []ImageType{ImageTypePrimary, ImageTypeProfile},
		ExcludePersonTypes:      []string{"a", "b"},
		PersonTypes:             []string{"a", "b"},
		ParentId:                "v-ParentId",
		AppearsInItemId:         "v-AppearsInItemId",
		UserId:                  "v-UserId",
		EnableImages:            new(true),
	})
	if client.StatusCode(err) != 418 || result.HttpResponse == nil {
		t.Errorf("an undocumented status = %v, %+v", err, result.HttpResponse)
	}
}

func TestOperationGetPersonsComplete(t *testing.T) {
	t.Parallel()

	// the three ways a walk ends: the total is reached, an empty page when the
	// server reports no total, and a page shorter than the limit
	for _, tc := range []struct {
		name         string
		total, pages int
		options      GetPersonsOperationOptions
		items        int
		starts       []string
	}{
		{"total reached", 2, 9, GetPersonsOperationOptions{Limit: new(1)}, 2, []string{"", "1"}},
		{"empty page", 0, 2, GetPersonsOperationOptions{Limit: new(1)}, 2, []string{"", "1", "2"}},
		{"short page", 0, 9, GetPersonsOperationOptions{Limit: new(2)}, 1, []string{""}},
	} {
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()

			c, starts := pagedServer(t, "startIndex", "{}", tc.total, tc.pages)
			result, err := c.GetPersonsComplete(t.Context(), tc.options)
			if err != nil {
				t.Fatal(err)
			}
			if len(result.Items) != tc.items || !slices.Equal(*starts, tc.starts) || result.LatestHttpResponse == nil {
				t.Errorf("Complete = %d items over pages starting %q, want %d over %q", len(result.Items), *starts, tc.items, tc.starts)
			}
		})
	}
}
