// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"errors"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// Client is a client for the Jellyfin API; each of its operations is a method.
// Requests go through Client.Client, the shared base client.
type Client struct {
	Client *client.Client
}

// New returns a client for the Jellyfin server at baseURL (scheme and host,
// optionally a path prefix) that authenticates with an API key or a user
// access token.
func New(baseURL, token string) (*Client, error) {
	if token == "" {
		return nil, errors.New("API key is required")
	}
	c, err := client.New(baseURL, client.JellyfinToken(token))
	if err != nil {
		return nil, err
	}

	return &Client{Client: c}, nil
}
