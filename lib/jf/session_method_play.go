// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"net/url"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// PlayOperationResponse is the result of Play.
type PlayOperationResponse struct {
	HttpResponse *http.Response
}

// PlayOperationOptions holds the query and header parameters of Play.
type PlayOperationOptions struct {
	// The type of play command to issue (PlayNow, PlayNext, PlayLast). Clients who have not yet implemented play next and play last may play now.
	PlayCommand PlayCommand

	// The ids of the items to play, comma delimited.
	ItemIds []string

	// The starting position of the first item.
	StartPositionTicks int64

	// Optional. The media source id.
	MediaSourceId string

	// Optional. The index of the audio stream to play.
	AudioStreamIndex int

	// Optional. The index of the subtitle stream to play.
	SubtitleStreamIndex int

	// Optional. The start index.
	StartIndex int
}

// ToHeaders returns the header parameters the options set.
func (o PlayOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o PlayOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.PlayCommand != "" {
		out.Append("playCommand", string(o.PlayCommand))
	}
	for _, v := range o.ItemIds {
		out.Append("itemIds", v)
	}
	if o.StartPositionTicks != 0 {
		out.Append("startPositionTicks", strconv.FormatInt(o.StartPositionTicks, 10))
	}
	if o.MediaSourceId != "" {
		out.Append("mediaSourceId", o.MediaSourceId)
	}
	if o.AudioStreamIndex != 0 {
		out.Append("audioStreamIndex", strconv.Itoa(o.AudioStreamIndex))
	}
	if o.SubtitleStreamIndex != 0 {
		out.Append("subtitleStreamIndex", strconv.Itoa(o.SubtitleStreamIndex))
	}
	if o.StartIndex != 0 {
		out.Append("startIndex", strconv.Itoa(o.StartIndex))
	}
	return &out
}

// Play calls POST /Sessions/{sessionId}/Playing. Instructs a session to play an item.
func (c Client) Play(ctx context.Context, sessionId string, options PlayOperationOptions) (result PlayOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusNoContent,
		},
		HttpMethod:    http.MethodPost,
		OptionsObject: options,
		Path:          fmt.Sprintf("/Sessions/%s/Playing", url.PathEscape(sessionId)),
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	return
}
