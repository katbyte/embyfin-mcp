// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"net/http"
	"slices"
	"testing"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

func TestOperationGetLiveTvChannels(t *testing.T) {
	t.Parallel()

	c, s := newOperationServer(t, 200, "application/json", "{}")
	result, err := c.GetLiveTvChannels(t.Context(), GetLiveTvChannelsOperationOptions{
		Type:                  ChannelTypeTV,
		UserId:                "v-UserId",
		StartIndex:            new(7),
		IsMovie:               new(true),
		IsSeries:              new(true),
		IsNews:                new(true),
		IsKids:                new(true),
		IsSports:              new(true),
		Limit:                 new(7),
		IsFavorite:            new(true),
		IsLiked:               new(true),
		IsDisliked:            new(true),
		EnableImages:          new(true),
		ImageTypeLimit:        new(7),
		EnableImageTypes:      []ImageType{ImageTypePrimary, ImageTypeProfile},
		Fields:                []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		EnableUserData:        new(true),
		SortBy:                []ItemSortBy{ItemSortByDefault, ItemSortByIndexNumber},
		SortOrder:             SortOrderAscending,
		EnableFavoriteSorting: new(true),
		AddCurrentProgram:     new(true),
	})
	if err != nil {
		t.Fatal(err)
	}
	r, body := s.only(t)
	_ = body
	expectRequest(t, r, http.MethodGet, "/LiveTv/Channels")
	expectQuery(t, r, "type", "TV")
	expectQuery(t, r, "userId", "v-UserId")
	expectQuery(t, r, "startIndex", "7")
	expectQuery(t, r, "isMovie", "true")
	expectQuery(t, r, "isSeries", "true")
	expectQuery(t, r, "isNews", "true")
	expectQuery(t, r, "isKids", "true")
	expectQuery(t, r, "isSports", "true")
	expectQuery(t, r, "limit", "7")
	expectQuery(t, r, "isFavorite", "true")
	expectQuery(t, r, "isLiked", "true")
	expectQuery(t, r, "isDisliked", "true")
	expectQuery(t, r, "enableImages", "true")
	expectQuery(t, r, "imageTypeLimit", "7")
	expectQuery(t, r, "enableImageTypes", "Primary", "Profile")
	expectQuery(t, r, "fields", "AirTime", "SpecialFeatureCount")
	expectQuery(t, r, "enableUserData", "true")
	expectQuery(t, r, "sortBy", "Default", "IndexNumber")
	expectQuery(t, r, "sortOrder", "Ascending")
	expectQuery(t, r, "enableFavoriteSorting", "true")
	expectQuery(t, r, "addCurrentProgram", "true")
	if result.HttpResponse == nil || result.HttpResponse.StatusCode != 200 {
		t.Fatalf("HttpResponse = %+v", result.HttpResponse)
	}
	if result.Model == nil {
		t.Error("the model was not decoded")
	}

	// an answer that does not decode is an error, with the response
	c, _ = newOperationServer(t, 200, "application/json", "<html>")
	result, err = c.GetLiveTvChannels(t.Context(), GetLiveTvChannelsOperationOptions{
		Type:                  ChannelTypeTV,
		UserId:                "v-UserId",
		StartIndex:            new(7),
		IsMovie:               new(true),
		IsSeries:              new(true),
		IsNews:                new(true),
		IsKids:                new(true),
		IsSports:              new(true),
		Limit:                 new(7),
		IsFavorite:            new(true),
		IsLiked:               new(true),
		IsDisliked:            new(true),
		EnableImages:          new(true),
		ImageTypeLimit:        new(7),
		EnableImageTypes:      []ImageType{ImageTypePrimary, ImageTypeProfile},
		Fields:                []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		EnableUserData:        new(true),
		SortBy:                []ItemSortBy{ItemSortByDefault, ItemSortByIndexNumber},
		SortOrder:             SortOrderAscending,
		EnableFavoriteSorting: new(true),
		AddCurrentProgram:     new(true),
	})
	if err == nil || client.StatusCode(err) != 0 || result.HttpResponse == nil {
		t.Errorf("an answer that does not decode = %v", err)
	}

	// a status the operation does not document is an error, with the response
	c, _ = newOperationServer(t, 418, "text/plain", "no")
	result, err = c.GetLiveTvChannels(t.Context(), GetLiveTvChannelsOperationOptions{
		Type:                  ChannelTypeTV,
		UserId:                "v-UserId",
		StartIndex:            new(7),
		IsMovie:               new(true),
		IsSeries:              new(true),
		IsNews:                new(true),
		IsKids:                new(true),
		IsSports:              new(true),
		Limit:                 new(7),
		IsFavorite:            new(true),
		IsLiked:               new(true),
		IsDisliked:            new(true),
		EnableImages:          new(true),
		ImageTypeLimit:        new(7),
		EnableImageTypes:      []ImageType{ImageTypePrimary, ImageTypeProfile},
		Fields:                []ItemFields{ItemFieldsAirTime, ItemFieldsSpecialFeatureCount},
		EnableUserData:        new(true),
		SortBy:                []ItemSortBy{ItemSortByDefault, ItemSortByIndexNumber},
		SortOrder:             SortOrderAscending,
		EnableFavoriteSorting: new(true),
		AddCurrentProgram:     new(true),
	})
	if client.StatusCode(err) != 418 || result.HttpResponse == nil {
		t.Errorf("an undocumented status = %v, %+v", err, result.HttpResponse)
	}
}

func TestOperationGetLiveTvChannelsComplete(t *testing.T) {
	t.Parallel()

	// the three ways a walk ends: the total is reached, an empty page when the
	// server reports no total, and a page shorter than the limit
	for _, tc := range []struct {
		name         string
		total, pages int
		options      GetLiveTvChannelsOperationOptions
		items        int
		starts       []string
	}{
		{"total reached", 2, 9, GetLiveTvChannelsOperationOptions{Limit: new(1)}, 2, []string{"", "1"}},
		{"empty page", 0, 2, GetLiveTvChannelsOperationOptions{Limit: new(1)}, 2, []string{"", "1", "2"}},
		{"short page", 0, 9, GetLiveTvChannelsOperationOptions{Limit: new(2)}, 1, []string{""}},
	} {
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()

			c, starts := pagedServer(t, "startIndex", "{}", tc.total, tc.pages)
			result, err := c.GetLiveTvChannelsComplete(t.Context(), tc.options)
			if err != nil {
				t.Fatal(err)
			}
			if len(result.Items) != tc.items || !slices.Equal(*starts, tc.starts) || result.LatestHttpResponse == nil {
				t.Errorf("Complete = %d items over pages starting %q, want %d over %q", len(result.Items), *starts, tc.items, tc.starts)
			}
		})
	}
}
