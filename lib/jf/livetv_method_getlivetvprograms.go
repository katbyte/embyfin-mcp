// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetLiveTvProgramsOperationResponse is the result of GetLiveTvPrograms.
type GetLiveTvProgramsOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetLiveTvProgramsOperationOptions holds the query and header parameters of GetLiveTvPrograms.
type GetLiveTvProgramsOperationOptions struct {
	// The channels to return guide information for.
	ChannelIds []string

	// Optional. Filter by user id.
	UserId string

	// Optional. The minimum premiere start date.
	MinStartDate string

	// Optional. Filter by programs that have completed airing, or not.
	HasAired *bool

	// Optional. Filter by programs that are currently airing, or not.
	IsAiring *bool

	// Optional. The maximum premiere start date.
	MaxStartDate string

	// Optional. The minimum premiere end date.
	MinEndDate string

	// Optional. The maximum premiere end date.
	MaxEndDate string

	// Optional. Filter for movies.
	IsMovie *bool

	// Optional. Filter for series.
	IsSeries *bool

	// Optional. Filter for news.
	IsNews *bool

	// Optional. Filter for kids.
	IsKids *bool

	// Optional. Filter for sports.
	IsSports *bool

	// Optional. The record index to start at. All items with a lower index will be dropped from the results.
	StartIndex int

	// Optional. The maximum number of records to return.
	Limit int

	// Optional. Specify one or more sort orders, comma delimited. Options: Name, StartDate.
	SortBy []ItemSortBy

	// Sort Order - Ascending,Descending.
	SortOrder []SortOrder

	// The genres to return guide information for.
	Genres []string

	// The genre ids to return guide information for.
	GenreIds []string

	// Optional. Include image information in output.
	EnableImages *bool

	// Optional. The max number of images to return, per image type.
	ImageTypeLimit int

	// Optional. The image types to include in the output.
	EnableImageTypes []ImageType

	// Optional. Include user data.
	EnableUserData *bool

	// Optional. Filter by series timer id.
	SeriesTimerId string

	// Optional. Filter by library series id.
	LibrarySeriesId string

	// Optional. Specify additional fields of information to return in the output.
	Fields []ItemFields

	// Retrieve total record count.
	EnableTotalRecordCount *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetLiveTvProgramsOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetLiveTvProgramsOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	for _, v := range o.ChannelIds {
		out.Append("channelIds", v)
	}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	if o.MinStartDate != "" {
		out.Append("minStartDate", o.MinStartDate)
	}
	if o.HasAired != nil {
		out.Append("hasAired", strconv.FormatBool(*o.HasAired))
	}
	if o.IsAiring != nil {
		out.Append("isAiring", strconv.FormatBool(*o.IsAiring))
	}
	if o.MaxStartDate != "" {
		out.Append("maxStartDate", o.MaxStartDate)
	}
	if o.MinEndDate != "" {
		out.Append("minEndDate", o.MinEndDate)
	}
	if o.MaxEndDate != "" {
		out.Append("maxEndDate", o.MaxEndDate)
	}
	if o.IsMovie != nil {
		out.Append("isMovie", strconv.FormatBool(*o.IsMovie))
	}
	if o.IsSeries != nil {
		out.Append("isSeries", strconv.FormatBool(*o.IsSeries))
	}
	if o.IsNews != nil {
		out.Append("isNews", strconv.FormatBool(*o.IsNews))
	}
	if o.IsKids != nil {
		out.Append("isKids", strconv.FormatBool(*o.IsKids))
	}
	if o.IsSports != nil {
		out.Append("isSports", strconv.FormatBool(*o.IsSports))
	}
	if o.StartIndex != 0 {
		out.Append("startIndex", strconv.Itoa(o.StartIndex))
	}
	if o.Limit != 0 {
		out.Append("limit", strconv.Itoa(o.Limit))
	}
	for _, v := range o.SortBy {
		out.Append("sortBy", string(v))
	}
	for _, v := range o.SortOrder {
		out.Append("sortOrder", string(v))
	}
	for _, v := range o.Genres {
		out.Append("genres", v)
	}
	for _, v := range o.GenreIds {
		out.Append("genreIds", v)
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.ImageTypeLimit != 0 {
		out.Append("imageTypeLimit", strconv.Itoa(o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	if o.SeriesTimerId != "" {
		out.Append("seriesTimerId", o.SeriesTimerId)
	}
	if o.LibrarySeriesId != "" {
		out.Append("librarySeriesId", o.LibrarySeriesId)
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	if o.EnableTotalRecordCount != nil {
		out.Append("enableTotalRecordCount", strconv.FormatBool(*o.EnableTotalRecordCount))
	}
	return &out
}

// GetLiveTvPrograms calls GET /LiveTv/Programs. Gets available live tv epgs.
func (c Client) GetLiveTvPrograms(ctx context.Context, options GetLiveTvProgramsOperationOptions) (result GetLiveTvProgramsOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HttpMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          "/LiveTv/Programs",
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}

// GetLiveTvProgramsCompleteResult is every result GetLiveTvProgramsComplete loaded.
type GetLiveTvProgramsCompleteResult struct {
	LatestHttpResponse *http.Response
	Items              []BaseItemDto
}

// GetLiveTvProgramsComplete calls GetLiveTvPrograms page by page from options.StartIndex until every
// result is loaded. options.Limit is the page size, client.DefaultPageSize when
// unset.
func (c Client) GetLiveTvProgramsComplete(ctx context.Context, options GetLiveTvProgramsOperationOptions) (result GetLiveTvProgramsCompleteResult, err error) {
	if options.Limit <= 0 {
		options.Limit = client.DefaultPageSize
	}
	for {
		var page GetLiveTvProgramsOperationResponse
		page, err = c.GetLiveTvPrograms(ctx, options)
		result.LatestHttpResponse = page.HttpResponse
		if err != nil {
			err = fmt.Errorf("loading results: %w", err)
			return
		}
		if page.Model == nil || len(page.Model.Items) == 0 {
			return
		}
		result.Items = append(result.Items, page.Model.Items...)
		options.StartIndex += len(page.Model.Items)
		// a short page is the last; so is reaching the total, when the server
		// reports one (some Emby lists report 0 whatever they hold)
		if len(page.Model.Items) < options.Limit || page.Model.TotalRecordCount > 0 && options.StartIndex >= page.Model.TotalRecordCount {
			return
		}
	}
}
