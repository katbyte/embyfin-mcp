// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetResumeItemsOperationResponse is the result of GetResumeItems.
type GetResumeItemsOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetResumeItemsOperationOptions holds the query and header parameters of GetResumeItems.
type GetResumeItemsOperationOptions struct {
	// The user id.
	UserId string

	// The start index.
	StartIndex int

	// The item limit.
	Limit int

	// The search term.
	SearchTerm string

	// Specify this to localize the search to a specific item or folder. Omit to use the root.
	ParentId string

	// Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines.
	Fields []ItemFields

	// Optional. Filter by MediaType. Allows multiple, comma delimited.
	MediaTypes []MediaType

	// Optional. Include user data.
	EnableUserData *bool

	// Optional. The max number of images to return, per image type.
	ImageTypeLimit int

	// Optional. The image types to include in the output.
	EnableImageTypes []ImageType

	// Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
	ExcludeItemTypes []BaseItemKind

	// Optional. If specified, results will be filtered based on the item type. This allows multiple, comma delimited.
	IncludeItemTypes []BaseItemKind

	// Optional. Enable the total record count.
	EnableTotalRecordCount *bool

	// Optional. Include image information in output.
	EnableImages *bool

	// Optional. Whether to exclude the currently active sessions.
	ExcludeActiveSessions *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetResumeItemsOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetResumeItemsOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	if o.StartIndex != 0 {
		out.Append("startIndex", strconv.Itoa(o.StartIndex))
	}
	if o.Limit != 0 {
		out.Append("limit", strconv.Itoa(o.Limit))
	}
	if o.SearchTerm != "" {
		out.Append("searchTerm", o.SearchTerm)
	}
	if o.ParentId != "" {
		out.Append("parentId", o.ParentId)
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	for _, v := range o.MediaTypes {
		out.Append("mediaTypes", string(v))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	if o.ImageTypeLimit != 0 {
		out.Append("imageTypeLimit", strconv.Itoa(o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	for _, v := range o.ExcludeItemTypes {
		out.Append("excludeItemTypes", string(v))
	}
	for _, v := range o.IncludeItemTypes {
		out.Append("includeItemTypes", string(v))
	}
	if o.EnableTotalRecordCount != nil {
		out.Append("enableTotalRecordCount", strconv.FormatBool(*o.EnableTotalRecordCount))
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.ExcludeActiveSessions != nil {
		out.Append("excludeActiveSessions", strconv.FormatBool(*o.ExcludeActiveSessions))
	}
	return &out
}

// GetResumeItems calls GET /UserItems/Resume. Gets items based on a query.
func (c Client) GetResumeItems(ctx context.Context, options GetResumeItemsOperationOptions) (result GetResumeItemsOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HttpMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          "/UserItems/Resume",
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}

// GetResumeItemsCompleteResult is every result GetResumeItemsComplete loaded.
type GetResumeItemsCompleteResult struct {
	LatestHttpResponse *http.Response
	Items              []BaseItemDto
}

// GetResumeItemsComplete calls GetResumeItems page by page from options.StartIndex until every
// result is loaded. options.Limit is the page size, client.DefaultPageSize when
// unset.
func (c Client) GetResumeItemsComplete(ctx context.Context, options GetResumeItemsOperationOptions) (result GetResumeItemsCompleteResult, err error) {
	if options.Limit <= 0 {
		options.Limit = client.DefaultPageSize
	}
	for {
		var page GetResumeItemsOperationResponse
		page, err = c.GetResumeItems(ctx, options)
		result.LatestHttpResponse = page.HttpResponse
		if err != nil {
			err = fmt.Errorf("loading results: %w", err)
			return
		}
		if page.Model == nil || len(page.Model.Items) == 0 {
			return
		}
		result.Items = append(result.Items, page.Model.Items...)
		options.StartIndex += len(page.Model.Items)
		// a short page is the last; so is reaching the total, when the server
		// reports one (some Emby lists report 0 whatever they hold)
		if len(page.Model.Items) < options.Limit || page.Model.TotalRecordCount > 0 && options.StartIndex >= page.Model.TotalRecordCount {
			return
		}
	}
}
