// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"net/url"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetMusicGenreImageByIndexOperationResponse is the result of GetMusicGenreImageByIndex.
//
// The operation answers image/*, left unread in HttpResponse.Body, which
// the caller must close.
type GetMusicGenreImageByIndexOperationResponse struct {
	HttpResponse *http.Response
}

// GetMusicGenreImageByIndexOperationOptions holds the query and header parameters of GetMusicGenreImageByIndex.
type GetMusicGenreImageByIndexOperationOptions struct {
	// Optional. Supply the cache tag from the item object to receive strong caching headers.
	Tag string

	// Determines the output format of the image - original,gif,jpg,png.
	Format ImageFormat

	// The maximum image width to return.
	MaxWidth int

	// The maximum image height to return.
	MaxHeight int

	// Optional. Percent to render for the percent played overlay.
	PercentPlayed float64

	// Optional. Unplayed count overlay to render.
	UnplayedCount int

	// The fixed image width to return.
	Width int

	// The fixed image height to return.
	Height int

	// Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases.
	Quality int

	// Width of box to fill.
	FillWidth int

	// Height of box to fill.
	FillHeight int

	// Optional. Blur image.
	Blur int

	// Optional. Apply a background color for transparent images.
	BackgroundColor string

	// Optional. Apply a foreground layer on top of the image.
	ForegroundLayer string
}

// ToHeaders returns the header parameters the options set.
func (o GetMusicGenreImageByIndexOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetMusicGenreImageByIndexOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.Tag != "" {
		out.Append("tag", o.Tag)
	}
	if o.Format != "" {
		out.Append("format", string(o.Format))
	}
	if o.MaxWidth != 0 {
		out.Append("maxWidth", strconv.Itoa(o.MaxWidth))
	}
	if o.MaxHeight != 0 {
		out.Append("maxHeight", strconv.Itoa(o.MaxHeight))
	}
	if o.PercentPlayed != 0 {
		out.Append("percentPlayed", strconv.FormatFloat(o.PercentPlayed, 'f', -1, 64))
	}
	if o.UnplayedCount != 0 {
		out.Append("unplayedCount", strconv.Itoa(o.UnplayedCount))
	}
	if o.Width != 0 {
		out.Append("width", strconv.Itoa(o.Width))
	}
	if o.Height != 0 {
		out.Append("height", strconv.Itoa(o.Height))
	}
	if o.Quality != 0 {
		out.Append("quality", strconv.Itoa(o.Quality))
	}
	if o.FillWidth != 0 {
		out.Append("fillWidth", strconv.Itoa(o.FillWidth))
	}
	if o.FillHeight != 0 {
		out.Append("fillHeight", strconv.Itoa(o.FillHeight))
	}
	if o.Blur != 0 {
		out.Append("blur", strconv.Itoa(o.Blur))
	}
	if o.BackgroundColor != "" {
		out.Append("backgroundColor", o.BackgroundColor)
	}
	if o.ForegroundLayer != "" {
		out.Append("foregroundLayer", o.ForegroundLayer)
	}
	return &out
}

// GetMusicGenreImageByIndex calls GET /MusicGenres/{name}/Images/{imageType}/{imageIndex}. Get music genre image by name.
func (c Client) GetMusicGenreImageByIndex(ctx context.Context, name string, imageType ImageType, imageIndex int, options GetMusicGenreImageByIndexOperationOptions) (result GetMusicGenreImageByIndexOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HttpMethod:     http.MethodGet,
		OptionsObject:  options,
		Path:           fmt.Sprintf("/MusicGenres/%s/Images/%s/%d", url.PathEscape(name), url.PathEscape(string(imageType)), imageIndex),
		StreamResponse: true,
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	return
}
