// Code generated by internal/pandorest; DO NOT EDIT.

package jf

import (
	"context"
	"fmt"
	"net/http"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetLiveTvChannelsOperationResponse is the result of GetLiveTvChannels.
type GetLiveTvChannelsOperationResponse struct {
	HttpResponse *http.Response
	Model        *BaseItemDtoQueryResult
}

// GetLiveTvChannelsOperationOptions holds the query and header parameters of GetLiveTvChannels.
type GetLiveTvChannelsOperationOptions struct {
	// Optional. Filter by channel type.
	Type ChannelType

	// Optional. Filter by user and attach user data.
	UserId string

	// Optional. The record index to start at. All items with a lower index will be dropped from the results.
	StartIndex *int

	// Optional. Filter for movies.
	IsMovie *bool

	// Optional. Filter for series.
	IsSeries *bool

	// Optional. Filter for news.
	IsNews *bool

	// Optional. Filter for kids.
	IsKids *bool

	// Optional. Filter for sports.
	IsSports *bool

	// Optional. The maximum number of records to return.
	Limit *int

	// Optional. Filter by channels that are favorites, or not.
	IsFavorite *bool

	// Optional. Filter by channels that are liked, or not.
	IsLiked *bool

	// Optional. Filter by channels that are disliked, or not.
	IsDisliked *bool

	// Optional. Include image information in output.
	EnableImages *bool

	// Optional. The max number of images to return, per image type.
	ImageTypeLimit *int

	// "Optional. The image types to include in the output.
	EnableImageTypes []ImageType

	// Optional. Specify additional fields of information to return in the output.
	Fields []ItemFields

	// Optional. Include user data.
	EnableUserData *bool

	// Optional. Key to sort by.
	SortBy []ItemSortBy

	// Optional. Sort order.
	SortOrder SortOrder

	// Optional. Incorporate favorite and like status into channel sorting.
	EnableFavoriteSorting *bool

	// Optional. Adds current program info to each channel.
	AddCurrentProgram *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetLiveTvChannelsOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetLiveTvChannelsOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.Type != "" {
		out.Append("type", string(o.Type))
	}
	if o.UserId != "" {
		out.Append("userId", o.UserId)
	}
	if o.StartIndex != nil {
		out.Append("startIndex", strconv.Itoa(*o.StartIndex))
	}
	if o.IsMovie != nil {
		out.Append("isMovie", strconv.FormatBool(*o.IsMovie))
	}
	if o.IsSeries != nil {
		out.Append("isSeries", strconv.FormatBool(*o.IsSeries))
	}
	if o.IsNews != nil {
		out.Append("isNews", strconv.FormatBool(*o.IsNews))
	}
	if o.IsKids != nil {
		out.Append("isKids", strconv.FormatBool(*o.IsKids))
	}
	if o.IsSports != nil {
		out.Append("isSports", strconv.FormatBool(*o.IsSports))
	}
	if o.Limit != nil {
		out.Append("limit", strconv.Itoa(*o.Limit))
	}
	if o.IsFavorite != nil {
		out.Append("isFavorite", strconv.FormatBool(*o.IsFavorite))
	}
	if o.IsLiked != nil {
		out.Append("isLiked", strconv.FormatBool(*o.IsLiked))
	}
	if o.IsDisliked != nil {
		out.Append("isDisliked", strconv.FormatBool(*o.IsDisliked))
	}
	if o.EnableImages != nil {
		out.Append("enableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.ImageTypeLimit != nil {
		out.Append("imageTypeLimit", strconv.Itoa(*o.ImageTypeLimit))
	}
	for _, v := range o.EnableImageTypes {
		out.Append("enableImageTypes", string(v))
	}
	for _, v := range o.Fields {
		out.Append("fields", string(v))
	}
	if o.EnableUserData != nil {
		out.Append("enableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	for _, v := range o.SortBy {
		out.Append("sortBy", string(v))
	}
	if o.SortOrder != "" {
		out.Append("sortOrder", string(o.SortOrder))
	}
	if o.EnableFavoriteSorting != nil {
		out.Append("enableFavoriteSorting", strconv.FormatBool(*o.EnableFavoriteSorting))
	}
	if o.AddCurrentProgram != nil {
		out.Append("addCurrentProgram", strconv.FormatBool(*o.AddCurrentProgram))
	}
	return &out
}

// GetLiveTvChannels calls GET /LiveTv/Channels. Gets available live tv channels.
func (c Client) GetLiveTvChannels(ctx context.Context, options GetLiveTvChannelsOperationOptions) (result GetLiveTvChannelsOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
		},
		HTTPMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          "/LiveTv/Channels",
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	var model BaseItemDtoQueryResult
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}

// GetLiveTvChannelsCompleteResult is every result GetLiveTvChannelsComplete loaded.
type GetLiveTvChannelsCompleteResult struct {
	LatestHttpResponse *http.Response
	Items              []BaseItemDto
}

// GetLiveTvChannelsComplete calls GetLiveTvChannels page by page from options.StartIndex until every
// result is loaded. options.Limit is the page size, client.DefaultPageSize when
// unset.
func (c Client) GetLiveTvChannelsComplete(ctx context.Context, options GetLiveTvChannelsOperationOptions) (result GetLiveTvChannelsCompleteResult, err error) {
	limit := client.DefaultPageSize
	if options.Limit != nil && *options.Limit > 0 {
		limit = *options.Limit
	}
	options.Limit = &limit
	start := 0
	if options.StartIndex != nil {
		start = *options.StartIndex
	}
	for {
		var page GetLiveTvChannelsOperationResponse
		page, err = c.GetLiveTvChannels(ctx, options)
		result.LatestHttpResponse = page.HttpResponse
		if err != nil {
			err = fmt.Errorf("loading results: %w", err)
			return
		}
		if page.Model == nil || len(page.Model.Items) == 0 {
			return
		}
		result.Items = append(result.Items, page.Model.Items...)
		start += len(page.Model.Items)
		options.StartIndex = &start
		// a short page is the last; so is reaching the total, when the server
		// reports one (some Emby lists report 0 whatever they hold)
		if len(page.Model.Items) < limit || page.Model.TotalRecordCount > 0 && start >= page.Model.TotalRecordCount {
			return
		}
	}
}
