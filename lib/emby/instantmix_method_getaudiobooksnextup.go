// Code generated by internal/pandorest; DO NOT EDIT.

package emby

import (
	"context"
	"fmt"
	"net/http"
	"strconv"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

// GetAudioBooksNextUpOperationResponse is the result of GetAudioBooksNextUp.
type GetAudioBooksNextUpOperationResponse struct {
	HttpResponse *http.Response
	Model        *QueryResultBaseItemDto
}

// GetAudioBooksNextUpOperationOptions holds the query and header parameters of GetAudioBooksNextUp.
type GetAudioBooksNextUpOperationOptions struct {
	// User Id
	UserId string

	// Optional. The record index to start at. All items with a lower index will be dropped from the results.
	StartIndex *int

	// Optional. The maximum number of records to return
	Limit *int

	// Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimeted. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls
	Fields string

	// Optional. Filter by series id
	AlbumId string

	// Specify this to localize the search to a specific item or folder. Omit to use the root
	ParentId string

	// Optional, include image information in output
	EnableImages *bool

	// Optional, the max number of images to return, per image type
	ImageTypeLimit *int

	// Optional. The image types to include in the output.
	EnableImageTypes string

	// Optional, include user data
	EnableUserData *bool
}

// ToHeaders returns the header parameters the options set.
func (o GetAudioBooksNextUpOperationOptions) ToHeaders() *client.Headers {
	out := client.Headers{}
	return &out
}

// ToQuery returns the query parameters the options set.
func (o GetAudioBooksNextUpOperationOptions) ToQuery() *client.QueryParams {
	out := client.QueryParams{}
	if o.UserId != "" {
		out.Append("UserId", o.UserId)
	}
	if o.StartIndex != nil {
		out.Append("StartIndex", strconv.Itoa(*o.StartIndex))
	}
	if o.Limit != nil {
		out.Append("Limit", strconv.Itoa(*o.Limit))
	}
	if o.Fields != "" {
		out.Append("Fields", o.Fields)
	}
	if o.AlbumId != "" {
		out.Append("AlbumId", o.AlbumId)
	}
	if o.ParentId != "" {
		out.Append("ParentId", o.ParentId)
	}
	if o.EnableImages != nil {
		out.Append("EnableImages", strconv.FormatBool(*o.EnableImages))
	}
	if o.ImageTypeLimit != nil {
		out.Append("ImageTypeLimit", strconv.Itoa(*o.ImageTypeLimit))
	}
	if o.EnableImageTypes != "" {
		out.Append("EnableImageTypes", o.EnableImageTypes)
	}
	if o.EnableUserData != nil {
		out.Append("EnableUserData", strconv.FormatBool(*o.EnableUserData))
	}
	return &out
}

// GetAudioBooksNextUp calls GET /AudioBooks/NextUp. Gets a list of next up episodes.
func (c Client) GetAudioBooksNextUp(ctx context.Context, options GetAudioBooksNextUpOperationOptions) (result GetAudioBooksNextUpOperationResponse, err error) {
	opts := client.RequestOptions{
		ExpectedStatusCodes: []int{
			http.StatusOK,
			http.StatusNoContent,
		},
		HTTPMethod:    http.MethodGet,
		OptionsObject: options,
		Path:          "/AudioBooks/NextUp",
	}

	req, err := c.Client.NewRequest(ctx, opts)
	if err != nil {
		return
	}

	var resp *client.Response
	resp, err = req.Execute(ctx)
	if resp != nil {
		result.HttpResponse = resp.Response
	}
	if err != nil {
		return
	}

	if resp.StatusCode == http.StatusNoContent {
		return
	}

	var model QueryResultBaseItemDto
	result.Model = &model
	if err = resp.Unmarshal(result.Model); err != nil {
		return
	}

	return
}

// GetAudioBooksNextUpCompleteResult is every result GetAudioBooksNextUpComplete loaded.
type GetAudioBooksNextUpCompleteResult struct {
	LatestHttpResponse *http.Response
	Items              []BaseItemDto
}

// GetAudioBooksNextUpComplete calls GetAudioBooksNextUp page by page from options.StartIndex until every
// result is loaded. options.Limit is the page size, client.DefaultPageSize when
// unset.
func (c Client) GetAudioBooksNextUpComplete(ctx context.Context, options GetAudioBooksNextUpOperationOptions) (result GetAudioBooksNextUpCompleteResult, err error) {
	limit := client.DefaultPageSize
	if options.Limit != nil && *options.Limit > 0 {
		limit = *options.Limit
	}
	options.Limit = &limit
	start := 0
	if options.StartIndex != nil {
		start = *options.StartIndex
	}
	for {
		var page GetAudioBooksNextUpOperationResponse
		page, err = c.GetAudioBooksNextUp(ctx, options)
		result.LatestHttpResponse = page.HttpResponse
		if err != nil {
			err = fmt.Errorf("loading results: %w", err)
			return
		}
		if page.Model == nil || len(page.Model.Items) == 0 {
			return
		}
		result.Items = append(result.Items, page.Model.Items...)
		start += len(page.Model.Items)
		options.StartIndex = &start
		// a short page is the last; so is reaching the total, when the server
		// reports one (some Emby lists report 0 whatever they hold)
		if len(page.Model.Items) < limit || page.Model.TotalRecordCount > 0 && start >= page.Model.TotalRecordCount {
			return
		}
	}
}
