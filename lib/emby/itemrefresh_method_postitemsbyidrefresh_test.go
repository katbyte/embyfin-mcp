// Code generated by internal/pandorest; DO NOT EDIT.

package emby

import (
	"encoding/json"
	"net/http"
	"testing"

	"github.com/katbyte/embyfin-mcp/lib/client"
)

func TestOperationPostItemsByIdRefresh(t *testing.T) {
	t.Parallel()

	c, s := newOperationServer(t, 200, "", "")
	result, err := c.PostItemsByIdRefresh(t.Context(), "p/id", BaseRefreshRequest{}, PostItemsByIdRefreshOperationOptions{
		Recursive:           new(true),
		MetadataRefreshMode: MetadataRefreshModeValidationOnly,
		ImageRefreshMode:    MetadataRefreshModeValidationOnly,
		ReplaceAllMetadata:  new(true),
		ReplaceAllImages:    new(true),
	})
	if err != nil {
		t.Fatal(err)
	}
	r, body := s.only(t)
	_ = body
	expectRequest(t, r, http.MethodPost, "/Items/p%2Fid/Refresh")
	expectQuery(t, r, "Recursive", "true")
	expectQuery(t, r, "MetadataRefreshMode", "ValidationOnly")
	expectQuery(t, r, "ImageRefreshMode", "ValidationOnly")
	expectQuery(t, r, "ReplaceAllMetadata", "true")
	expectQuery(t, r, "ReplaceAllImages", "true")
	if got := r.Header.Get("Content-Type"); got != "application/json" {
		t.Errorf("Content-Type = %q, want %q", got, "application/json")
	}
	if !json.Valid([]byte(body)) {
		t.Errorf("body = %q, want JSON", body)
	}
	if result.HttpResponse == nil || result.HttpResponse.StatusCode != 200 {
		t.Fatalf("HttpResponse = %+v", result.HttpResponse)
	}

	// a status the operation does not document is an error, with the response
	c, _ = newOperationServer(t, 418, "text/plain", "no")
	result, err = c.PostItemsByIdRefresh(t.Context(), "p/id", BaseRefreshRequest{}, PostItemsByIdRefreshOperationOptions{
		Recursive:           new(true),
		MetadataRefreshMode: MetadataRefreshModeValidationOnly,
		ImageRefreshMode:    MetadataRefreshModeValidationOnly,
		ReplaceAllMetadata:  new(true),
		ReplaceAllImages:    new(true),
	})
	if client.StatusCode(err) != 418 || result.HttpResponse == nil {
		t.Errorf("an undocumented status = %v, %+v", err, result.HttpResponse)
	}
}
